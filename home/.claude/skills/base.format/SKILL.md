---
name: <skill-id>
description: <한 문장 트리거 — Claude가 이 스킬을 언제 호출할지 결정하는 신호. 「무엇을, 언제, 왜」가 한 문장에 들어가야 함. 너무 일반적이면 호출되지 않고, 너무 좁으면 다른 스킬과 충돌.>
argument-hint: '<슬래시 커맨드 입력창에 보일 자리표시자. 예: "[PVPLUS-번호] [task|bug] [요약]". 없으면 생략.>'
model: sonnet
context: fork
tools: <스킬이 실제로 사용하는 도구만 콤마로. 예: Bash, Read, Grep, mcp__claude_ai_Atlassian__getJiraIssue>
---

> 💡 **이 파일에는 작성 가이드가 HTML 주석으로 숨겨져 있습니다.** Typora 소스 모드(⌘/) 또는 raw 텍스트 에디터로 확인하세요. 새 스킬을 만들 때는 가시 안내 + 모든 주석을 지우고 커밋합니다.

<!--
=======================================================================
 SKILL.md 작성 가이드 — 이 파일은 모든 스킬의 기준 템플릿이다.

 [사용법]
   1. 이 파일을 새 스킬 폴더에 복사: skills/<skill-id>/SKILL.md
   2. frontmatter와 각 섹션을 채운다.
   3. 비어 있는 선택 섹션은 통째로 삭제한다.
   4. 상단 가시 안내(💡 인용 블록)와 모든 주석을 지우고 커밋한다.

 [주석 컨벤션]
   가이드성 텍스트는 HTML 주석으로 감싼다 (단일/다중 라인 모두).
   - Typora 등 markdown 뷰어에서 깨끗하게 숨김 처리됨 — rendered view 노이즈 X.
   - 표준 markdown 형식이라 모든 도구에서 일관 동작.
   - 가이드는 템플릿 작성자용 — 최종 스킬 사용자에게 보일 필요 없음.
   - 가이드 존재 자체는 파일 상단의 가시 안내(💡 인용 블록)로 알린다.
   - 새 스킬 만들 때 가시 안내 + 모든 HTML 주석 삭제 후 커밋.

 [규칙 섹션 — 강제도로 2단 분리]
   LLM은 평면 bullet에서 위계를 잃기 쉬우므로 섹션으로 가른다.

   ## 반드시 지킬 규칙   위반 시 작업 중단. 안전·정합성·핵심 컨벤션·파괴적 행동 금지.
   ## 기본 규칙          명시적 사유 없으면 따르는 기본 동작.

   각 섹션 안에서 긍정/부정은 bullet 문장으로 표현:
   - "X 한다"
   - "X 하지 않는다"

   부연 설명(— 왜)은 붙이지 않는다. 규칙 자체가 모호하면 규칙을 다시 쓴다.
   같은 강제도 안에서 짝이 되는 규칙은 인접 배치.

 [frontmatter 필드]
   - name: 슬래시 커맨드명과 일치. kebab-case + 점 허용 (e.g., git.commit)
   - description: 호출 트리거의 핵심. 「무엇을, 언제, 왜」 한 문장.
                  너무 일반적이면 호출 안 되고, 너무 좁으면 충돌.
   - argument-hint: 입력 자리표시자. 인자 받지 않으면 생략 가능.
   - model: 스킬 실행 시 사용할 모델. **기본값은 sonnet**.
            값: sonnet / opus / haiku / inherit (현재 세션 모델 유지).
            해당 턴에만 적용되고 끝나면 세션 모델로 자동 복귀.
            바꿀 이유가 명확할 때만 변경(예: 무거운 점검 → opus, 짧은 작업 → haiku).
   - context: 스킬 실행 컨텍스트. **기본값은 fork** — isolated subagent에서 실행.
            값: fork (격리, 부모 대화 미상속) / 미지정 (부모 컨텍스트 상속).
            fork면 토큰 절약 + 단독 수행 보장 + CLAUDE.md는 여전히 로드됨.
            상속이 정말 필요한 경우만 생략(예: 직전 작업 결과를 이어받아야 하는 스킬).
   - agent: context: fork일 때 사용할 subagent 타입. **기본값은 general-purpose**.
            값: general-purpose / Explore / Plan / 등 등록된 agent 이름.
            특별한 도구 제약이 필요한 경우만 명시.
   - tools: 스킬이 실제로 사용하는 도구만 콤마로 나열.
            허용 풀: Bash, Read, Grep, Edit, Write, MCP 도구.
            

 [섹션 구조 — 코어 4 + 옵션 1 + 추가 섹션]
   모든 스킬은 다음 4개를 코어로 가정한다. 순서도 이 순서를 따른다.

   # 제목 + 컨텍스트 단락
   ## 워크플로우
   ## 반드시 지킬 규칙
   ## 기본 규칙
   ## 마무리 규칙         무엇을 검증하고 무엇을 보고할지 (출력 계약/spec).

   사용자 응답이 필요하거나 fork→main 후속 동작이 있으면 옵션 섹션을 추가:

   ## 리턴 형식          사용자 응답 prompt + main session 후속 동작 안내.

   마무리 규칙·리턴 형식은 짝이다 — 규칙은 무엇을 어떤 조건에 출력할지,
   리턴 형식은 그 결과의 실제 모양과 후속 동작. 사용자 응답이 필요한 fork 스킬은 둘 다 채운다.

   fork 출력은 리턴 형식만 부모로 흘러간다.
   분석·diff·파일 본문·MCP raw 응답·중간 추론은 부모로 흘리지 않는다 —
   토큰 격리의 핵심이 이 분리에 있다.

   필요할 때만 코어 아래에 추가 섹션을 붙인다. 코어 사이에 끼우지 않는다.
   추가 섹션 권장 순서:

   ## 체크리스트            보고형 스킬에서 워크플로우 한 단계가
                            "아래 항목을 점검한다"가 될 때 분리.
   ## 예시                 추상 규칙만으로 모호한 부분에만.
   ## 참고                 외부 자료가 있을 때만.

   위 목록 외 섹션도 필요하면 자유롭게 추가 가능.
   기준: "이 섹션이 없으면 스킬이 못 굴러가는가?" — 답이 yes면 추가.
   불필요한 섹션은 만들지 않는다. 코어도 정말 비면 비워둔다(채우려 짜내지 않음).

 [공통 작성 원칙]
   - 명령은 코드 블록으로 박는다 — 자연어 설명만 있으면 LLM이 추측.
   - 한 단계 = 한 가지 일. 병렬 가능한 묶음만 "병렬로 실행:"으로 명시.
   - 본문에서 같은 규칙을 반복하지 말 것 — 한 번이면 충분.
   - description과 워크플로우 첫 줄이 중복되지 않도록.
=======================================================================
-->

# <자연어 제목>: $ARGUMENTS

<!--
2~3줄: 이 스킬의 본질, 호출 시 가정하는 상황, 사용자 의도 해석 기준.
description을 풀어서 설명한다.
-->

<!-- ================ 코어 4개 (모든 스킬 공통) ================ -->

## 워크플로우

1. <상태 확인 / 입력 검증으로 시작>
2. <…>
3. <…>
4. <마무리 규칙을 참고하여 리턴 형식이 있으면 출력하고 종료>

## 반드시 지킬 규칙

- <강제 규칙: X 한다>
- <강제 금지: X 하지 않는다>

## 기본 규칙

- <기본 동작: X 한다>
- <기본 회피: X 하지 않는다>

## 마무리 규칙

- <검증: 결과 출력 직전 점검할 조건. `BLOCKED: 내용`으로 종료>
- <보고: 헤딩 위쪽에만 남길 분석·진행 로그. 부모 컨텍스트로 안 흐름. 필요 없으면 생략>


<!--
=============== 리턴 형식 섹션 (필요할 때만) ==============
context:fork나 결과, 응답이 필요한 섹션에서 사용. 안 쓰면 통째로 삭제.
================================================
-->

## 리턴 형식

```
진행하려면 (ㅇ/y), 중단하려면 (ㄴ/n)을 적어주세요.
```

진행 응답을 받으면 리턴을 참고해 부모 컨텍스트가 진행 될 수 있도록한다.


<!--
================ 추가 섹션 (필요할 때만) ================
아래 섹션은 필요한 것만 골라 쓴다. 안 쓰면 통째로 삭제.
모두 코어 4개 뒤에 둔다 — 코어 사이에 끼우지 않는다.
여기 없는 섹션도 스킬에 필요하면 코어 뒤에 자유롭게 추가 가능.
================================================
-->

## 체크리스트

<!--
보고형 스킬에서 워크플로우 한 단계가 "아래 체크리스트를 수행한다"가 될 때 사용.
주제별로 묶어서 [ ] 항목으로. 한 항목은 한 가지 점검.
-->

**<주제 1>:**
- [ ] <점검 항목 — 무엇을 보는지 명확히>
- [ ] <…>

**<주제 2>:**
- [ ] <…>

## 예시

<!-- "입력 → 출력" 형태가 가장 효과적. -->

- 입력: <…>
  출력: <…>

## 참고

- <…>
