---
name: <agent-id>
description: <한 문장 트리거 — 메인 어시스턴트(또는 다른 에이전트)가 이 에이전트에 위임해야 할 상황을 식별하는 신호. 「무엇을, 언제, 왜」가 한 문장에 들어가야 함. 너무 일반적이면 호출되지 않고, 너무 좁으면 다른 에이전트와 충돌.>
model: sonnet
tools: <쉼표로 구분된 도구 목록. 예) Bash, Read, Edit, Grep, Glob. 비우면 부모의 모든 도구 상속.>
---

> 💡 **이 파일에는 작성 가이드가 HTML 주석으로 숨겨져 있습니다.** Typora 소스 모드(⌘/) 또는 raw 텍스트 에디터로 확인하세요. 새 에이전트를 만들 때는 가시 안내 + 모든 주석을 지우고 커밋합니다.

<!--
==========================================================================
 AGENT.md 작성 가이드 — 모든 subagent의 기준 템플릿이다.

 [사용법]
   1. 이 파일을 새 에이전트 폴더에 복사: agents/<orchestrators|workers>/<agent-id>/AGENT.md
   2. frontmatter와 각 섹션을 채운다.
   3. 비어 있는 추가 섹션은 통째로 삭제한다.
   4. 상단 가시 안내(💡 인용 블록)와 모든 주석을 지우고 커밋한다.
   5. 배포: `~/.claude/agents/<agent-id>` 심링크 후 `claude` 재시작.

 [주석 컨벤션]
   가이드성 텍스트는 HTML 주석으로 감싼다 (단일/다중 라인 모두).
   - Typora 등 markdown 뷰어에서 깨끗하게 숨김 처리됨 — rendered view 노이즈 X.
   - 표준 markdown 형식이라 모든 도구에서 일관 동작.
   - 가이드는 템플릿 작성자용 — 최종 사용자에게 보일 필요 없음.
   - 가이드 존재 자체는 파일 상단의 가시 안내(💡 인용 블록)로 알린다.
   - 새 에이전트 만들 때 가시 안내 + 모든 HTML 주석 삭제 후 커밋.

 [규칙 섹션 — 강제도로 2단 분리]
   LLM은 평면 bullet에서 위계를 잃기 쉬우므로 섹션으로 가른다.

   ## 반드시 지킬 규칙   위반 시 작업 중단. 안전·정합성·핵심 컨벤션·파괴적 행동 금지.
   ## 기본 규칙          명시적 사유 없으면 따르는 기본 동작.

   각 항목은 "X 한다 — 왜" / "X 하지 않는다 — 왜" 형식.
   이유는 한 줄로 짧게. 짝이 되는 규칙은 인접 배치.

 [frontmatter 필드]
   - name: 에이전트의 고유 식별자. kebab-case + 점 허용 (e.g., git.commit).
           이 이름이 `@agent-<name>` 호출에 쓰임.
   - description: 위임 트리거의 핵심. 부모가 "이 작업을 누구에게 줄까" 판단하는 근거.
                  너무 일반적이면 호출 안 되고, 너무 좁으면 충돌.
   - model: 실행 시 사용할 모델. **기본값 sonnet**.
            값: sonnet / opus / haiku / inherit (부모 모델 유지).
   - tools: 이 에이전트에 허용할 도구 목록(쉼표 구분).
            비우면 부모 컨텍스트의 모든 도구 상속.
            **최소 권한 원칙** — 필요한 것만 명시. 파괴적 도구는 신중히.

 [호출 모델 — 에이전트는 isolated context에서 실행된다]
   - 사용자/부모는 `@agent-<name> <prompt>` 형식으로 호출.
   - 에이전트는 **새 컨텍스트 윈도우**에서 시작 — 부모 대화 미상속.
   - 결과는 마지막 메시지로 부모에 반환. 양방향 대화 없음 (한 번 호출, 한 번 보고).
   - 따라서 SKILL.md의 "단독 수행 원칙"이 본질적으로 강제됨 — 부모 컨텍스트에 기댈 수 없음.

 [섹션 구조 — 코어 4 + 추가 섹션]
   모든 에이전트는 다음 4개를 코어로 가정한다. 순서도 이 순서를 따른다.

   # 제목 + 컨텍스트 단락
   ## 워크플로우
   ## 반드시 지킬 규칙
   ## 기본 규칙

   필요할 때만 코어 4개 아래에 추가 섹션을 붙인다. 코어 사이에 끼우지 않는다.
   추가 섹션 권장 순서:

   ## 체크리스트            보고형 에이전트(review/audit 등)에서
                            워크플로우 한 단계가 "아래 항목을 점검한다"가 될 때.
   ## 출력 포맷             부모에게 반환할 결과 구조 명시.
   ## 예시                 추상 규칙만으로 모호한 부분에만.
   ## 참고                 외부 자료가 있을 때만.

   위 목록 외 섹션도 필요하면 코어 뒤에 자유롭게 추가 가능.
   기준: "이 섹션이 없으면 에이전트가 못 굴러가는가?" — 답이 yes면 추가.
   불필요한 섹션은 만들지 않는다.

 [공통 작성 원칙]
   - 명령은 코드 블록으로 박는다 — 자연어 설명만 있으면 LLM이 추측.
   - 한 단계 = 한 가지 일. 병렬 가능한 묶음만 "병렬로 실행:"으로 명시.
   - 본문에서 같은 규칙을 반복하지 말 것 — 한 번이면 충분.
   - description과 워크플로우 첫 줄이 중복되지 않도록.
   - 출력 포맷이 있으면 거기서 정확히 정의 — 부모는 이 구조를 그대로 받음.
==========================================================================
-->

# <자연어 제목>

<!--
2~3줄: 이 에이전트의 본질, 위임 시 가정하는 상황,
부모로부터 받는 prompt를 어떻게 해석하는지.
description을 풀어서 설명한다.
-->

<!-- ================ 코어 4개 (모든 에이전트 공통) ================ -->

## 워크플로우

1. <부모 prompt 해석 / 입력 검증으로 시작>
2. <…>
3. <…>
4. <검증 / 부모에게 결과 보고로 종료>

## 반드시 지킬 규칙

- <강제 규칙: X 한다> — <왜>
- <강제 금지: X 하지 않는다> — <왜>

## 기본 규칙

- <기본 동작: X 한다> — <왜>
- <기본 회피: X 하지 않는다> — <왜>

<!--
================ 추가 섹션 (필요할 때만) ================
아래 섹션은 필요한 것만 골라 쓴다. 안 쓰면 통째로 삭제.
모두 코어 4개 뒤에 둔다 — 코어 사이에 끼우지 않는다.
여기 없는 섹션도 필요하면 코어 뒤에 자유롭게 추가 가능.
============================================================
-->

## 체크리스트

<!--
보고형 에이전트에서 워크플로우 한 단계가 "아래 체크리스트를 수행한다"가 될 때 사용.
주제별로 묶어서 [ ] 항목으로. 한 항목은 한 가지 점검.
-->

**<주제 1>:**
- [ ] <점검 항목 — 무엇을 보는지 명확히>
- [ ] <…>

**<주제 2>:**
- [ ] <…>

## 출력 포맷

```
<부모에게 반환할 결과 구조 — 헤더, 섹션, 필드까지 정확히>
```

## 예시

<!-- "입력(부모 prompt) → 출력(반환 결과)" 형태가 가장 효과적. -->

- 입력: <부모로부터 받은 prompt 예>
  출력: <에이전트가 반환하는 결과 예>

## 참고

- <…>
