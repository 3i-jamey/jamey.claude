# Claude Code local state (repo-root only — the runtime cache for THIS repo)
/.claude/

# Obsidian per-machine state (layout, mobile workspace)
.obsidian/workspace.json
.obsidian/workspace-mobile.json

# macOS noise (everywhere)
.DS_Store
